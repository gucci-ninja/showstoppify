<%= form_with(model: nomination_list) do |form| %>
  <% if nomination_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nomination_list.errors.count, "error") %> prohibited this nomination_list from being saved:</h2>

      <ul>
        <% nomination_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :token %>
    <%= form.text_field :token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
